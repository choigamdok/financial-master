<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>재무설계 마스터 - 통합 재무 계산기</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700;800;900&display=swap"
        rel="stylesheet">

    <style>
        * {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
        }

        /* 토스 스타일 변수 */
        :root {
            --toss-blue: #0064FF;
            --toss-blue-dark: #0052CC;
            --toss-black: #191F28;
            --toss-gray: #4E5968;
            --toss-green: #00C73C;
            --toss-red: #FF5A5F;
            --toss-bg: #F9FAFB;
        }

        body {
            background: var(--toss-bg);
            color: var(--toss-black);
        }

        /* 탭 전환 애니메이션 */
        .tab-content {
            display: none;
            animation: fadeIn 0.3s ease-in-out;
        }

        .tab-content.active {
            display: block;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* 토스 스타일 카드 */
        .toss-card {
            background: white;
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.08);
            border: 1px solid #f0f0f0;
            transition: all 0.2s;
        }

        .toss-card:hover {
            box-shadow: 0 4px 24px rgba(0, 0, 0, 0.12);
        }

        /* 토스 버튼 */
        .toss-btn {
            border-radius: 12px;
            font-weight: 700;
            transition: all 0.2s;
            padding: 16px 24px;
            font-size: 16px;
        }

        .toss-btn-primary {
            background: var(--toss-blue);
            color: white;
        }

        .toss-btn-primary:hover {
            background: var(--toss-blue-dark);
            transform: translateY(-2px);
        }

        /* 토스 인풋 */
        .toss-input {
            border: 2px solid #E5E8EB;
            border-radius: 12px;
            padding: 14px 16px;
            font-size: 16px;
            transition: border 0.2s;
            width: 100%;
        }

        .toss-input:focus {
            border-color: var(--toss-blue);
            outline: none;
        }

        /* 숫자 강조 */
        .toss-number {
            font-weight: 800;
            font-variant-numeric: tabular-nums;
            letter-spacing: -0.02em;
        }

        /* 스크롤바 */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        ::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 3px;
        }
    </style>
</head>

<body class="min-h-screen">
    <!-- Header -->
    <header class="bg-gradient-to-r from-blue-600 to-blue-500 text-white sticky top-0 z-50 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="flex items-center justify-between">
                <div>
                    <h1 class="text-2xl sm:text-3xl font-black tracking-tight">재무설계 마스터</h1>
                    <p class="text-blue-100 text-sm mt-1">모든 재무 계산을 한 곳에서</p>
                </div>
                <div class="hidden sm:block text-right">
                    <p class="text-sm font-bold tracking-wide">Financial Master Calculator</p>
                    <p class="text-xs text-blue-100 mt-0.5">금쇼맘의 금융스쿨</p>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation Tabs -->
    <nav class="bg-white border-b border-gray-200 sticky top-[88px] sm:top-[96px] z-40 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex overflow-x-auto gap-2 py-3 scrollbar-hide">
                <!-- 1. 라이프 사이클 -->
                <button onclick="switchTab('lifecycle', event)"
                    class="tab-btn active whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>📅 라이프 사이클</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Life Cycle</span>
                </button>

                <!-- 2. 황금 거위 만들기 -->
                <button onclick="switchTab('retirement', event)"
                    class="tab-btn whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>🎯 황금 거위 만들기</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Golden Goose</span>
                </button>

                <!-- 3. 은퇴 설계 -->
                <button onclick="switchTab('withdrawal', event)"
                    class="tab-btn whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>💳 은퇴 설계</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Retirement Plan</span>
                </button>

                <!-- 4. 복리의 마법 -->
                <button onclick="switchTab('monthly-compound', event)"
                    class="tab-btn whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>📈 복리의 마법</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Compound Magic</span>
                </button>

                <!-- 5. 수익율 비교 -->
                <button onclick="switchTab('compound', event)"
                    class="tab-btn whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>💰 수익율 비교</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Return Compare</span>
                </button>

                <!-- 6. 게으름 비용 -->
                <button onclick="switchTab('delay', event)"
                    class="tab-btn whitespace-nowrap px-4 py-2.5 rounded-full font-bold text-sm transition-all flex flex-col items-center leading-tight">
                    <span>⏰ 게으름 비용</span>
                    <span class="text-[10px] font-normal opacity-75 mt-0.5">Cost of Delay</span>
                </button>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

        <!-- Tab 1: 복리 성장 -->
        <div id="tab-compound" class="tab-content active">
            <div class="mb-8 bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-6 border border-blue-100">
                <div class="flex items-center gap-3 mb-3">
                    <div
                        class="w-12 h-12 bg-blue-600 rounded-xl flex items-center justify-center text-white font-black text-xl shadow-lg">
                        1
                    </div>
                    <div>
                        <h2 class="text-2xl font-black text-gray-900">💰 수익율 비교</h2>
                        <p class="text-xs text-gray-500 font-semibold uppercase tracking-wider mt-0.5">Wealth Growth
                            Simulator</p>
                    </div>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    두 가지 투자 플랜을 비교하고 복리 효과를 확인하세요.
                    <span class="font-bold text-blue-700">작은 이율 차이</span>도 장기적으로는
                    <span class="font-bold text-purple-700">엄청난 자산 격차</span>를 만들어냅니다.
                </p>
            </div>

            <!-- 입력 섹션 -->
            <div class="toss-card mb-6">
                <div class="flex items-center gap-3 mb-4">
                    <div class="w-1.5 h-6 bg-gray-900 rounded-full"></div>
                    <h3 class="text-lg font-bold text-gray-900">Simulation Parameters</h3>
                    <span class="text-xs text-gray-400 font-medium ml-auto">모든 금액 단위: 원(KRW)</span>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
                    <div>
                        <label class="block text-xs font-bold text-gray-500 uppercase tracking-wide mb-2">현재 나이</label>
                        <div class="relative">
                            <input type="number" id="comp-age" value="35" class="toss-input text-right pr-10">
                            <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">세</span>
                        </div>
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-gray-500 uppercase tracking-wide mb-2">투자 기간</label>
                        <div class="relative">
                            <input type="number" id="comp-duration" value="30" class="toss-input text-right pr-10">
                            <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">년</span>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">예상 종료: <span id="comp-end-age"
                                class="font-bold">65</span>세</p>
                    </div>
                    <div class="md:col-span-2">
                        <label class="block text-xs font-bold text-gray-500 uppercase tracking-wide mb-2">초기 투자
                            원금</label>
                        <div class="relative">
                            <input type="text" id="comp-principal" value="50,000,000"
                                class="toss-input text-right pr-10 text-lg font-bold">
                            <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 font-bold">원</span>
                        </div>
                    </div>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6 pt-6 border-t">
                    <div class="bg-blue-50 p-5 rounded-xl hover:bg-blue-100 transition-colors border border-blue-100">
                        <div class="flex items-center gap-3 mb-3">
                            <div
                                class="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center text-blue-600">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                                </svg>
                            </div>
                            <div class="flex-1">
                                <span class="block text-xs font-bold text-gray-500 uppercase">Plan A</span>
                                <span class="block text-sm font-bold text-blue-900">보수적 운용 (Conservative)</span>
                            </div>
                            <span class="text-xs bg-blue-200 text-blue-800 px-2 py-1 rounded font-bold">안정형</span>
                        </div>
                        <div class="flex items-center gap-3">
                            <input type="number" id="comp-rateA" value="3.5" step="0.1"
                                class="toss-input text-right text-2xl font-bold text-blue-600">
                            <span class="text-xl font-bold text-blue-600">%</span>
                        </div>
                        <p class="text-xs text-gray-600 mt-2">예: 채권, 예금, 안정적 펀드</p>
                    </div>

                    <div
                        class="bg-amber-50 p-5 rounded-xl hover:bg-amber-100 transition-colors border border-amber-100">
                        <div class="flex items-center gap-3 mb-3">
                            <div
                                class="w-10 h-10 rounded-full bg-amber-100 flex items-center justify-center text-amber-600">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
                                </svg>
                            </div>
                            <div class="flex-1">
                                <span class="block text-xs font-bold text-gray-500 uppercase">Plan B</span>
                                <span class="block text-sm font-bold text-amber-900">적극적 운용 (Aggressive)</span>
                            </div>
                            <span class="text-xs bg-amber-200 text-amber-800 px-2 py-1 rounded font-bold">공격형</span>
                        </div>
                        <div class="flex items-center gap-3">
                            <input type="number" id="comp-rateB" value="7.0" step="0.1"
                                class="toss-input text-right text-2xl font-bold text-amber-600">
                            <span class="text-xl font-bold text-amber-600">%</span>
                        </div>
                        <p class="text-xs text-gray-600 mt-2">예: 주식, 부동산, 성장형 펀드</p>
                    </div>
                </div>
            </div>

            <!-- 결과 카드 -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
                <div class="toss-card bg-gradient-to-br from-blue-50 to-white border-blue-100">
                    <span class="text-xs font-bold text-blue-600 uppercase tracking-wider">Plan A</span>
                    <h3 class="text-3xl font-black text-blue-700 mt-2 mb-1 toss-number" id="comp-totalA">0원</h3>
                    <p class="text-sm text-gray-600">예상 최종 자산</p>
                    <div class="mt-4 pt-4 border-t border-blue-100">
                        <span class="text-xs text-gray-500">누적 수익률</span>
                        <p class="text-lg font-bold text-blue-600 mt-1"><span id="comp-accRateA">0</span>%</p>
                    </div>
                </div>

                <div class="toss-card bg-gradient-to-br from-amber-50 to-white border-amber-100">
                    <span class="text-xs font-bold text-amber-600 uppercase tracking-wider">Plan B</span>
                    <h3 class="text-3xl font-black text-amber-700 mt-2 mb-1 toss-number" id="comp-totalB">0원</h3>
                    <p class="text-sm text-gray-600">예상 최종 자산</p>
                    <div class="mt-4 pt-4 border-t border-amber-100">
                        <span class="text-xs text-gray-500">누적 수익률</span>
                        <p class="text-lg font-bold text-amber-600 mt-1"><span id="comp-accRateB">0</span>%</p>
                    </div>
                </div>

                <div class="toss-card bg-gradient-to-br from-gray-900 to-gray-800 text-white">
                    <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">차익 분석</span>
                    <h3 class="text-3xl font-black text-white mt-2 mb-1 toss-number" id="comp-diff">0원</h3>
                    <p class="text-sm text-gray-300">Plan B - Plan A</p>
                    <div class="mt-4 pt-4 border-t border-gray-700">
                        <span class="text-xs text-gray-400">이율 차이</span>
                        <p class="text-lg font-bold text-amber-400 mt-1"><span id="comp-rateDiff">0.0</span>%p</p>
                    </div>
                </div>
            </div>

            <!-- 차트 -->
            <div class="toss-card mb-6">
                <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6 gap-4">
                    <div>
                        <h3 class="text-lg font-bold text-gray-900">Asset Growth Projection</h3>
                        <p class="text-sm text-gray-500 mt-1">시간 경과에 따른 자산 가치 변화 비교</p>
                    </div>
                    <div class="flex gap-6">
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-blue-500 shadow-sm"></span>
                            <span class="text-xs font-bold text-gray-600">Plan A (보수적)</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <span class="w-3 h-3 rounded-full bg-amber-500 shadow-sm"></span>
                            <span class="text-xs font-bold text-gray-600">Plan B (적극적)</span>
                        </div>
                    </div>
                </div>
                <div class="relative" style="height: 400px;">
                    <canvas id="compChart"></canvas>
                </div>
            </div>

            <!-- 상세 테이블 -->
            <div class="toss-card">
                <h3 class="text-lg font-bold mb-4">연도별 상세 내역</h3>
                <div class="overflow-x-auto">
                    <div class="max-h-96 overflow-y-auto">
                        <table class="w-full text-sm">
                            <thead class="bg-gray-50 sticky top-0">
                                <tr class="text-gray-600">
                                    <th class="py-3 px-4 text-center font-bold">연도 / 나이</th>
                                    <th class="py-3 px-4 text-right font-bold text-blue-700">Plan A 자산</th>
                                    <th class="py-3 px-4 text-right text-gray-500">수익률</th>
                                    <th class="py-3 px-4 text-right font-bold text-amber-700">Plan B 자산</th>
                                    <th class="py-3 px-4 text-right text-gray-500">수익률</th>
                                    <th class="py-3 px-4 text-right font-bold text-gray-900">차액</th>
                                </tr>
                            </thead>
                            <tbody id="compTableBody" class="divide-y divide-gray-100">
                                <!-- JS로 생성 -->
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 2: 월 저축 복리 -->
        <div id="tab-monthly-compound" class="tab-content">
            <div class="mb-8 bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-6 border border-green-100">
                <div class="flex items-center gap-3 mb-3">
                    <div
                        class="w-12 h-12 bg-green-600 rounded-xl flex items-center justify-center text-white shadow-lg">
                        <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        </svg>
                    </div>
                    <div>
                        <h2 class="text-2xl font-black text-gray-900">📈 복리의 마법</h2>
                        <p class="text-xs text-gray-500 font-semibold uppercase tracking-wider mt-0.5">Monthly Savings
                            Compound Calculator</p>
                    </div>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    "지금 쓰는 푼돈이 미래의 목돈이 됩니다." 매월 조금씩 저축하면 복리 효과로 <span class="font-bold text-green-700">놀라운 자산</span>을 만들
                    수 있습니다!
                </p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- 입력 섹션 -->
                <div class="toss-card space-y-4">
                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-2">현재 나이</label>
                        <div class="relative">
                            <input type="number" id="mc-age" value="45" class="toss-input text-right pr-10">
                            <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">세</span>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-2">재무 목적</label>
                        <input type="text" id="mc-goal" placeholder="예: 내 집 마련, 세계일주" class="toss-input">
                    </div>

                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-2">월 저축액: <span id="mc-save-text"
                                class="text-green-600">300,000원</span></label>
                        <input type="range" id="mc-save-range" min="100000" max="5000000" step="50000" value="300000"
                            class="w-full accent-green-600 mb-2">
                        <input type="number" id="mc-save-input" value="300000" class="toss-input text-right">
                    </div>

                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-2">예상 수익률: <span id="mc-rate-text"
                                class="text-blue-600">5%</span></label>
                        <div class="flex items-center gap-2">
                            <input type="range" id="mc-rate" min="1" max="15" step="0.5" value="5"
                                class="w-full accent-blue-600">
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-bold text-gray-700 mb-2">투자 기간: <span id="mc-years-text"
                                class="text-purple-600">10년</span></label>
                        <input type="range" id="mc-years" min="5" max="40" step="1" value="10"
                            class="w-full accent-purple-600">
                        <p id="mc-end-age" class="text-xs text-purple-600 mt-1 text-right font-bold">예상 만기 나이: 55세</p>
                    </div>
                </div>

                <!-- 결과 & 차트 -->
                <div class="lg:col-span-2 toss-card bg-gray-50">
                    <div class="relative mb-4" style="height: 320px;">
                        <canvas id="mcChart"></canvas>
                    </div>

                    <div class="grid grid-cols-3 gap-3">
                        <div class="bg-white p-3 rounded-lg border shadow-sm text-center">
                            <p class="text-xs text-gray-500 mb-1">총 납입 원금</p>
                            <p id="mc-res-principal" class="font-bold text-gray-700 text-sm">0</p>
                        </div>
                        <div class="bg-blue-50 p-3 rounded-lg border border-blue-100 shadow-sm text-center">
                            <p class="text-xs text-blue-600 mb-1">총 이자 수익</p>
                            <p id="mc-res-interest" class="font-bold text-blue-700 text-sm">+0</p>
                        </div>
                        <div class="bg-indigo-50 p-3 rounded-lg border border-indigo-100 shadow-sm text-center">
                            <p id="mc-res-goal-label" class="text-xs text-indigo-600 mb-1">최종 자산</p>
                            <p id="mc-res-total" class="font-bold text-indigo-700 text-sm">0</p>
                        </div>
                    </div>

                    <div class="mt-4 text-center border-t pt-3">
                        <p class="text-sm text-gray-500">
                            총 수익률: <span id="mc-res-yield" class="font-bold text-green-600 text-lg">0%</span>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 3: 게으름 비용 -->

        <div id="tab-delay" class="tab-content">
            <div class="mb-6">
                <h2 class="text-2xl font-black text-gray-900 mb-2">⏰ 게으름 비용</h2>
                <p class="text-gray-600">오늘의 망설임이 미래의 가장 큰 비용입니다</p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
                <!-- 입력 섹션 -->
                <div class="toss-card">
                    <h3 class="text-lg font-bold mb-4">설정</h3>
                    <div class="space-y-4">
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">현재 나이</label>
                                <div class="relative">
                                    <input type="number" id="delay-age" value="45" class="toss-input text-right pr-10">
                                    <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">세</span>
                                </div>
                            </div>
                            <div>
                                <label class="block text-sm font-semibold text-gray-700 mb-2">재무 목적</label>
                                <input type="text" id="delay-goal" placeholder="예: 은퇴 자금" class="toss-input">
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">목표 금액</label>
                            <div class="relative">
                                <input type="number" id="delay-amount" value="100000000"
                                    class="toss-input text-right pr-10 text-lg font-bold">
                                <span class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">원</span>
                            </div>
                            <div class="flex gap-2 mt-2">
                                <button onclick="setDelayAmount(50000000)"
                                    class="px-3 py-1 text-xs bg-gray-100 hover:bg-gray-200 rounded-lg">5천만</button>
                                <button onclick="setDelayAmount(100000000)"
                                    class="px-3 py-1 text-xs bg-gray-100 hover:bg-gray-200 rounded-lg">1억</button>
                                <button onclick="setDelayAmount(300000000)"
                                    class="px-3 py-1 text-xs bg-gray-100 hover:bg-gray-200 rounded-lg">3억</button>
                            </div>
                        </div>

                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">총 준비 기간: <span
                                    id="delay-years-text" class="text-blue-600">20년</span></label>
                            <input type="range" id="delay-years" min="1" max="50" value="20"
                                class="w-full accent-blue-600">
                            <p class="text-xs text-gray-500 mt-1">목표 달성 나이: <span id="delay-end-age"
                                    class="font-bold">65</span>세</p>
                        </div>

                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">지연 기간: <span
                                    id="delay-delay-text" class="text-red-600">5년 뒤 시작</span></label>
                            <input type="range" id="delay-delay" min="1" max="19" value="5"
                                class="w-full accent-red-600">
                        </div>

                        <div>
                            <label class="block text-sm font-semibold text-gray-700 mb-2">수익률: <span
                                    id="delay-rate-text" class="text-green-600">6%</span></label>
                            <input type="range" id="delay-rate" min="2" max="10" step="0.5" value="6"
                                class="w-full accent-green-600 ">
                        </div>
                    </div>
                </div>

                <!-- 결과 비교 -->
                <div class="space-y-4">
                    <div class="toss-card bg-gradient-to-br from-blue-50 to-white border-blue-100">
                        <div class="flex justify-between items-center mb-3">
                            <h4 class="font-bold text-blue-900">✅ 지금 (<span id="delay-now-age">45</span>세) 시작하면</h4>
                            <span class="text-xs bg-blue-200 text-blue-800 px-2 py-1 rounded font-bold">BEST</span>
                        </div>
                        <div class="mb-4">
                            <p class="text-sm text-gray-600 mb-1">월 저축액 (<span id="delay-now-years">20</span>년)</p>
                            <p class="text-3xl font-black text-blue-700 toss-number" id="delay-now-monthly">0원</p>
                        </div>
                        <div class="pt-3 border-t border-blue-100">
                            <p class="text-xs text-gray-500">총 불입원금</p>
                            <p class="text-lg font-bold text-blue-600" id="delay-now-total">0원</p>
                        </div>
                    </div>

                    <div class="toss-card bg-gradient-to-br from-red-50 to-white border-red-100">
                        <div class="flex justify-between items-center mb-3">
                            <h4 class="font-bold text-red-900">⚠️ <span id="delay-delay-years">5</span>년 뒤 (<span
                                    id="delay-delay-age">50</span>세) 시작하면</h4>
                            <span class="text-xs bg-red-200 text-red-800 px-2 py-1 rounded font-bold">WARNING</span>
                        </div>
                        <div class="mb-4">
                            <p class="text-sm text-gray-600 mb-1">월 저축액 (<span id="delay-remain-years">15</span>년)</p>
                            <p class="text-3xl font-black text-red-700 toss-number" id="delay-delay-monthly">0원</p>
                        </div>
                        <div class="pt-3 border-t border-red-100">
                            <p class="text-xs text-gray-500">총 불입원금</p>
                            <p class="text-lg font-bold text-red-600" id="delay-delay-total">0원</p>
                        </div>
                    </div>

                    <div class="toss-card bg-gradient-to-br from-gray-900 to-gray-800 text-white text-center py-8">
                        <p class="text-sm text-gray-400 mb-2">게으름 비용 (<span id="delay-val">5</span>년 지연)</p>
                        <p class="text-4xl font-black text-yellow-400 toss-number mb-2" id="delay-cost">0원</p>
                        <p class="text-xs text-gray-400">더 내야 하는 돈</p>
                    </div>
                </div>
            </div>
        </div>


        <!-- Tab 3: 은퇴 설계 -->
        <div id="tab-retirement" class="tab-content">
            <div class="mb-6">
                <h2 class="text-2xl font-black text-gray-900 mb-2">🎯 황금 거위 만들기</h2>
                <p class="text-gray-600">원금은 지키고 배당으로만 평생 월급 받기 (연 7% 배당 기준)</p>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                <!-- 입력 섹션 -->
                <div class="space-y-6">
                    <!-- 1. 은퇴 목표 설정 -->
                    <div class="toss-card border-l-4 border-blue-500">
                        <div class="flex items-center gap-2 mb-5">
                            <div
                                class="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center text-white font-bold">
                                1</div>
                            <h3 class="text-xl font-black text-gray-900">은퇴 목표 설정</h3>
                        </div>

                        <div class="grid grid-cols-2 gap-4 mb-5">
                            <div class="bg-gray-50 p-4 rounded-xl">
                                <label class="block text-xs font-bold text-gray-500 mb-2 uppercase">현재 나이</label>
                                <div class="relative">
                                    <input type="number" id="ret-age" value="40"
                                        class="toss-input text-center text-2xl font-bold">
                                </div>
                            </div>
                            <div class="bg-gray-50 p-4 rounded-xl">
                                <label class="block text-xs font-bold text-gray-500 mb-2 uppercase">은퇴 나이</label>
                                <div class="relative">
                                    <input type="number" id="ret-retire-age" value="65"
                                        class="toss-input text-center text-2xl font-bold">
                                </div>
                            </div>
                        </div>

                        <div class="bg-blue-50 p-5 rounded-xl border-2 border-blue-200">
                            <label class="block text-sm font-bold text-blue-900 mb-3">💰 희망 월 수령액 (은퇴 생활비)</label>
                            <div class="relative">
                                <input type="number" id="ret-pension" value="3000000" step="100000"
                                    class="toss-input text-right pr-12 text-3xl font-black text-blue-700 bg-white">
                                <span
                                    class="absolute right-4 top-1/2 -translate-y-1/2 text-lg font-bold text-gray-500">원</span>
                            </div>
                        </div>

                        <div
                            class="mt-5 bg-gradient-to-r from-blue-500 to-indigo-600 p-6 rounded-2xl text-center shadow-lg">
                            <p class="text-xs text-white/80 mb-2 font-semibold uppercase tracking-wider">배당률 7% 기준 필요 목돈
                            </p>
                            <p class="text-sm font-bold text-yellow-300 mb-1">황금거위가 되려면?</p>
                            <p class="text-4xl font-black text-white toss-number" id="ret-target">0원</p>
                        </div>
                    </div>

                    <!-- 2. 저축 & 증식 플랜 -->
                    <div class="toss-card border-l-4 border-green-500">
                        <div class="flex items-center gap-2 mb-5">
                            <div
                                class="w-8 h-8 bg-green-500 rounded-lg flex items-center justify-center text-white font-bold">
                                2</div>
                            <h3 class="text-xl font-black text-gray-900">저축 & 증식 플랜</h3>
                        </div>

                        <!-- A. 모으는 기간 -->
                        <div class="mb-6 pb-6 border-b-2 border-gray-200">
                            <div class="flex justify-between items-center mb-3">
                                <label class="font-bold text-base text-green-700 flex items-center gap-2">
                                    <span class="text-2xl">🌱</span>
                                    A. 모으는 기간 (저축)
                                </label>
                                <span id="ret-save-badge"
                                    class="text-sm font-bold bg-green-100 text-green-800 px-3 py-1.5 rounded-full">10년</span>
                            </div>
                            <input type="range" id="ret-save-years" min="1" max="30" value="10"
                                class="w-full h-3 accent-green-600 mb-3">
                            <div class="bg-green-50 p-3 rounded-lg flex items-center justify-between">
                                <span class="text-sm font-semibold text-gray-700">기대 수익률</span>
                                <div class="flex items-center gap-2">
                                    <input type="number" id="ret-save-rate" value="5"
                                        class="w-20 p-2 border-2 border-green-300 rounded-lg text-right text-lg font-bold">
                                    <span class="text-lg font-bold text-green-700">%</span>
                                </div>
                            </div>
                        </div>

                        <!-- B. 불리는 기간 -->
                        <div>
                            <div class="flex justify-between items-center mb-3">
                                <label class="font-bold text-base text-purple-700 flex items-center gap-2">
                                    <span class="text-2xl">🚀</span>
                                    B. 불리는 기간 (거치)
                                </label>
                                <span id="ret-defer-badge"
                                    class="text-sm font-bold bg-purple-100 text-purple-800 px-3 py-1.5 rounded-full">15년</span>
                            </div>
                            <input type="range" id="ret-defer-years" min="0" max="30" value="15"
                                class="w-full h-3 accent-purple-600 mb-3">
                            <div class="bg-purple-50 p-3 rounded-lg flex items-center justify-between">
                                <span class="text-sm font-semibold text-gray-700">기대 수익률</span>
                                <div class="flex items-center gap-2">
                                    <input type="number" id="ret-defer-rate" value="5"
                                        class="w-20 p-2 border-2 border-purple-300 rounded-lg text-right text-lg font-bold">
                                    <span class="text-lg font-bold text-purple-700">%</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- 결과 섹션 -->
                <div
                    class="toss-card bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 text-white flex flex-col justify-center p-8 shadow-2xl border-4 border-yellow-400">
                    <div class="text-center mb-6">
                        <div class="inline-block bg-yellow-400/20 px-4 py-2 rounded-full mb-4">
                            <p class="text-yellow-400 text-xs font-bold uppercase tracking-widest">목표 달성 전략</p>
                        </div>
                    </div>

                    <div class="mb-8 text-center bg-black/30 rounded-2xl p-6 border-2 border-yellow-500/30">
                        <p class="text-yellow-300 text-sm font-semibold mb-2">💵 매월 이만큼만 저축하세요!</p>
                        <span id="ret-monthly"
                            class="text-6xl md:text-7xl font-black text-yellow-400 toss-number block">0원</span>
                        <p class="text-gray-300 mt-3 text-base font-medium">
                            <span id="ret-save-text">10</span>년 동안 매월 납입 시
                        </p>
                    </div>

                    <div class="bg-slate-800/80 rounded-xl p-5 text-sm space-y-4 border border-slate-700">
                        <div class="flex justify-between items-center py-2">
                            <span class="flex items-center gap-2 font-bold text-green-400 text-base">
                                <div class="w-4 h-4 rounded-full bg-green-500 shadow-lg shadow-green-500/50"></div>
                                <span>1단계: 목돈 만들기</span>
                            </span>
                            <span id="ret-fv-save" class="text-white font-bold">0 달성</span>
                        </div>

                        <div id="ret-step-2" class="flex justify-between items-center py-2">
                            <span class="flex items-center gap-2 font-bold text-purple-400 text-base">
                                <div class="w-4 h-4 rounded-full bg-purple-500 shadow-lg shadow-purple-500/50"></div>
                                <span>2단계: 굴리기</span>
                            </span>
                            <span id="ret-target-2" class="text-white font-bold">0 달성</span>
                        </div>

                        <div class="flex justify-between items-center border-t-2 border-yellow-500/30 pt-4 mt-2">
                            <span class="flex items-center gap-2 font-bold text-blue-400">
                                <div class="w-3 h-3 rounded-full bg-blue-500"></div> 3단계: 평생 월급
                            </span>
                            <span id="ret-pension-result" class="text-yellow-400 font-bold">매월 0</span>
                        </div>
                    </div>

                    <div id="ret-alert"
                        class="mt-4 flex items-center gap-2 text-orange-400 text-xs bg-orange-400/10 px-3 py-1 rounded-full hidden">
                        ⚠️ 주의: 기간 합계가 은퇴까지 남은 시간과 다릅니다.
                    </div>
                </div>
            </div>
        </div>

        <!-- Tab 4: 생애주기 -->
        <div id="tab-lifecycle" class="tab-content">
            <div class="mb-6 bg-gradient-to-r from-purple-50 to-pink-50 rounded-2xl p-6 border border-purple-100">
                <div class="flex items-center gap-3 mb-3">
                    <div
                        class="w-12 h-12 bg-purple-600 rounded-xl flex items-center justify-center text-white shadow-lg">
                        <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z">
                            </path>
                        </svg>
                    </div>
                    <div>
                        <h2 class="text-2xl font-black text-gray-900">📅 라이프 사이클</h2>
                        <p class="text-xs text-gray-500 font-semibold uppercase tracking-wider mt-0.5">Life Cycle
                            Financial Planner</p>
                    </div>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    인생의 각 단계별 재무 목표를 설정하고 타임라인으로 시각화하세요.
                </p>
            </div>

            <!-- iframe Container -->
            <div class="toss-card p-0 overflow-hidden" style="height: calc(100vh - 300px); min-height: 600px;">
                <iframe src="life_cycle.html" class="w-full h-full border-0" title="생애주기 플래너" loading="lazy">
                </iframe>
            </div>
        </div>

        <!-- Tab 5: 은퇴 인출 -->
        <div id="tab-withdrawal" class="tab-content">
            <div class="mb-6 bg-gradient-to-r from-cyan-50 to-blue-50 rounded-2xl p-6 border border-cyan-100">
                <div class="flex items-center gap-3 mb-3">
                    <div class="w-12 h-12 bg-cyan-600 rounded-xl flex items-center justify-center text-white shadow-lg">
                        <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z">
                            </path>
                        </svg>
                    </div>
                    <div>
                        <h2 class="text-2xl font-black text-gray-900">💳 은퇴 설계</h2>
                        <p class="text-xs text-gray-500 font-semibold uppercase tracking-wider mt-0.5">Retirement
                            Withdrawal Simulator</p>
                    </div>
                </div>
                <p class="text-gray-700 leading-relaxed">
                    준비된 은퇴 자금으로 나이대별 실제 수령액과 실질 가치를 미리 확인하세요.
                </p>
            </div>

            <!-- iframe Container -->
            <div class="toss-card p-0 overflow-hidden" style="height: calc(100vh - 300px); min-height: 600px;">
                <iframe src="retirement_withdrawal.html" class="w-full h-full border-0" title="은퇴 인출 시뮬레이터"
                    loading="lazy">
                </iframe>
            </div>
        </div>

    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-gray-400 py-12 mt-20">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h3 class="text-white font-bold text-lg mb-2">재무설계 마스터</h3>
            <p class="text-sm mb-4">모든 재무 계산을 한 곳에서 간편하게</p>
            <p class="text-xs text-gray-500">본 계산 결과는 예시이며 실제 투자 수익을 보장하지 않습니다.</p>
            <p class="text-xs text-gray-600 mt-6">© 2024-2026 All rights reserved.</p>
        </div>
    </footer>

    <script>
        // ===== 탭 전환 =====
        function switchTab(tabName, event) {
            // 모든 탭 숨기기
            document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
            document.getElementById('tab-' + tabName).classList.add('active');

            // 탭 버튼 스타일
            document.querySelectorAll('.tab-btn').forEach(btn => {
                btn.classList.remove('active', 'bg-blue-600', 'text-white', 'shadow-lg');
                btn.classList.add('bg-gray-100', 'text-gray-600');
            });

            // currentTarget을 사용하여 실제 버튼 요소를 가져옴 (span 클릭 시에도 작동)
            const clickedBtn = event.currentTarget;
            clickedBtn.classList.remove('bg-gray-100', 'text-gray-600');
            clickedBtn.classList.add('active', 'bg-blue-600', 'text-white', 'shadow-lg');
        }

        // ===== 복리 계산기 =====
        const compState = {
            age: 35,
            duration: 30,
            principal: 50000000,
            rateA: 3.5,
            rateB: 7.0,
            data: []
        };

        const compEls = {
            age: document.getElementById('comp-age'),
            duration: document.getElementById('comp-duration'),
            principal: document.getElementById('comp-principal'),
            rateA: document.getElementById('comp-rateA'),
            rateB: document.getElementById('comp-rateB'),
            endAge: document.getElementById('comp-end-age'),
            totalA: document.getElementById('comp-totalA'),
            accRateA: document.getElementById('comp-accRateA'),
            totalB: document.getElementById('comp-totalB'),
            accRateB: document.getElementById('comp-accRateB'),
            diff: document.getElementById('comp-diff'),
            rateDiff: document.getElementById('comp-rateDiff'),
            tableBody: document.getElementById('compTableBody'),
            canvas: document.getElementById('compChart')
        };

        let compChart = null;

        const formatMoney = (n) => new Intl.NumberFormat('ko-KR').format(Math.round(n));
        const parseMoney = (s) => parseInt(s.replace(/[^0-9]/g, '')) || 0;

        function calculateCompound() {
            compState.data = [];
            const { principal, rateA, rateB, duration, age } = compState;

            compEls.endAge.textContent = age + duration;
            compEls.rateDiff.textContent = Math.abs(rateB - rateA).toFixed(1);

            for (let i = 1; i <= duration; i++) {
                const totalA = principal * Math.pow((1 + rateA / 100), i);
                const totalB = principal * Math.pow((1 + rateB / 100), i);

                compState.data.push({
                    year: i,
                    currentAge: age + i,
                    totalA,
                    totalB,
                    accRateA: (totalA / principal) * 100,
                    accRateB: (totalB / principal) * 100,
                    diff: totalB - totalA
                });
            }

            updateCompoundUI();
        }

        function updateCompoundUI() {
            if (compState.data.length === 0) return;
            const final = compState.data[compState.data.length - 1];

            compEls.totalA.textContent = formatMoney(final.totalA) + '원';
            compEls.accRateA.textContent = final.accRateA.toFixed(0);

            compEls.totalB.textContent = formatMoney(final.totalB) + '원';
            compEls.accRateB.textContent = final.accRateB.toFixed(0);

            compEls.diff.textContent = formatMoney(Math.abs(final.diff)) + '원';

            renderCompoundTable();
            updateCompoundChart();
        }

        function renderCompoundTable() {
            compEls.tableBody.innerHTML = '';

            // 시작 행
            const startRow = `
                <tr class="text-gray-400 italic">
                    <td class="py-3 px-4 text-center">시작 (${compState.age}세)</td>
                    <td class="py-3 px-4 text-right">${formatMoney(compState.principal)}</td>
                    <td class="py-3 px-4 text-right">100%</td>
                    <td class="py-3 px-4 text-right">${formatMoney(compState.principal)}</td>
                    <td class="py-3 px-4 text-right">100%</td>
                    <td class="py-3 px-4 text-right">-</td>
                </tr>
            `;
            compEls.tableBody.insertAdjacentHTML('beforeend', startRow);

            compState.data.forEach((d, idx) => {
                const isMilestone = d.year % 5 === 0;
                const rowClass = isMilestone ? 'bg-blue-50 font-semibold' : 'hover:bg-gray-50';

                const row = `
                    <tr class="${rowClass}">
                        <td class="py-3 px-4 text-center">
                            <span class="text-gray-500">${d.year}년 후</span>
                            <span class="ml-2 bg-gray-100 px-2 py-1 rounded font-semibold">${d.currentAge}세</span>
                        </td>
                        <td class="py-3 px-4 text-right text-blue-700 font-semibold">${formatMoney(d.totalA)}</td>
                        <td class="py-3 px-4 text-right text-gray-500">${d.accRateA.toFixed(0)}%</td>
                        <td class="py-3 px-4 text-right text-amber-700 font-semibold">${formatMoney(d.totalB)}</td>
                        <td class="py-3 px-4 text-right text-gray-500">${d.accRateB.toFixed(0)}%</td>
                        <td class="py-3 px-4 text-right font-bold text-gray-900">+${formatMoney(d.diff)}</td>
                    </tr>
                `;
                compEls.tableBody.insertAdjacentHTML('beforeend', row);
            });
        }

        function updateCompoundChart() {
            const ctx = compEls.canvas.getContext('2d');

            if (compChart) compChart.destroy();

            compChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: compState.data.map(d => d.year + '년'),
                    datasets: [
                        {
                            label: 'Plan A (보수적)',
                            data: compState.data.map(d => d.totalA),
                            borderColor: '#2563eb',
                            backgroundColor: 'rgba(37, 99, 235, 0.1)',
                            borderWidth: 3,
                            fill: true,
                            tension: 0.4
                        },
                        {
                            label: 'Plan B (적극적)',
                            data: compState.data.map(d => d.totalB),
                            borderColor: '#d97706',
                            backgroundColor: 'rgba(217, 119, 6, 0.1)',
                            borderWidth: 3,
                            fill: true,
                            tension: 0.4
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    interaction: { mode: 'index', intersect: false },
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top',
                            labels: {
                                font: { size: 14, weight: 'bold' },
                                padding: 15
                            }
                        },
                        tooltip: {
                            backgroundColor: 'rgba(255, 255, 255, 0.95)',
                            titleColor: '#191F28',
                            bodyColor: '#4E5968',
                            borderColor: '#e2e8f0',
                            borderWidth: 1,
                            padding: 12,
                            callbacks: {
                                label: (ctx) => ' ' + ctx.dataset.label + ': ' + formatMoney(ctx.parsed.y) + '원'
                            }
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false },
                            ticks: { font: { size: 11 } }
                        },
                        y: {
                            grid: { color: '#f1f5f9' },
                            ticks: {
                                callback: (value) => {
                                    if (value >= 100000000) return (value / 100000000).toFixed(0) + '억';
                                    if (value >= 10000) return (value / 10000).toFixed(0) + '만';
                                    return value;
                                }
                            }
                        }
                    }
                }
            });
        }

        // 이벤트 리스너
        compEls.principal.addEventListener('input', (e) => {
            const v = parseMoney(e.target.value);
            if (!isNaN(v)) { compState.principal = v; calculateCompound(); }
        });
        compEls.principal.addEventListener('blur', (e) => {
            e.target.value = formatMoney(compState.principal);
        });

        ['age', 'duration'].forEach(key => {
            compEls[key].addEventListener('input', (e) => {
                let v = parseInt(e.target.value) || 0;
                if (v < 0) v = 0;
                compState[key] = v;
                calculateCompound();
            });
        });

        ['rateA', 'rateB'].forEach(key => {
            compEls[key].addEventListener('input', (e) => {
                let v = parseFloat(e.target.value) || 0;
                if (v < 0) v = 0;
                compState[key] = v;
                calculateCompound();
            });
        });

        // 초기화
        window.addEventListener('DOMContentLoaded', () => {
            compEls.principal.value = formatMoney(compState.principal);
            calculateCompound();
            calculateDelay(); // 게으름 비용 초기화
            updateMonthlyCompound(); // 월 저축 복리 초기화
        });

        // =================================================================
        // 2. 월 저축 복리 계산기 (Monthly Compound Calculator)
        // =================================================================
        const mcEls = {
            age: document.getElementById('mc-age'),
            goal: document.getElementById('mc-goal'),
            saveRange: document.getElementById('mc-save-range'),
            saveInput: document.getElementById('mc-save-input'),
            saveText: document.getElementById('mc-save-text'),
            rate: document.getElementById('mc-rate'),
            rateText: document.getElementById('mc-rate-text'),
            years: document.getElementById('mc-years'),
            yearsText: document.getElementById('mc-years-text'),
            endAge: document.getElementById('mc-end-age'),
            resPrincipal: document.getElementById('mc-res-principal'),
            resInterest: document.getElementById('mc-res-interest'),
            resGoalLabel: document.getElementById('mc-res-goal-label'),
            resTotal: document.getElementById('mc-res-total'),
            resYield: document.getElementById('mc-res-yield'),
            canvas: document.getElementById('mcChart')
        };

        let mcChart = null;

        function updateMonthlyCompound() {
            const age = Number(mcEls.age.value);
            const save = Number(mcEls.saveInput.value);
            const rate = Number(mcEls.rate.value);
            const years = Number(mcEls.years.value);
            const goalName = mcEls.goal.value;

            // Sync inputs
            mcEls.saveRange.value = save;
            mcEls.saveText.textContent = formatMoney(save) + '원';
            mcEls.rateText.textContent = rate + '%';
            mcEls.yearsText.textContent = years + '년';
            mcEls.endAge.textContent = `예상 만기 나이: ${age + years}세`;

            // Calculate FV with monthly contributions (Future Value of Annuity)
            const data = [];
            for (let i = 1; i <= years; i++) {
                const monthlyRate = rate / 100 / 12;
                const months = i * 12;
                const fv = save * ((Math.pow(1 + monthlyRate, months) - 1) / monthlyRate);
                const principal = save * 12 * i;
                data.push({
                    age: age + i,
                    year: i,
                    principal: Math.round(principal),
                    total: Math.round(fv),
                    interest: Math.round(fv - principal)
                });
            }

            const final = data[data.length - 1] || { principal: 0, total: 0, interest: 0 };
            mcEls.resPrincipal.textContent = formatMoney(final.principal);
            mcEls.resInterest.textContent = '+' + formatMoney(final.interest);
            mcEls.resTotal.textContent = formatMoney(final.total);
            const yieldPercent = final.principal > 0 ? ((final.interest / final.principal) * 100).toFixed(1) : 0;
            mcEls.resYield.textContent = yieldPercent + '%';

            if (goalName.trim()) {
                mcEls.resGoalLabel.textContent = goalName + ' 달성 금액';
            } else {
                mcEls.resGoalLabel.textContent = '최종 자산';
            }

            // Update Chart
            updateMCChart(data);
        }

        function updateMCChart(data) {
            if (mcChart) mcChart.destroy();

            const ctx = mcEls.canvas.getContext('2d');
            mcChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: data.map(d => d.year + '년차'),
                    datasets: [
                        {
                            label: '총 납입 원금',
                            data: data.map(d => d.principal),
                            borderColor: '#94a3b8',
                            backgroundColor: 'rgba(148, 163, 184, 0.1)',
                            borderWidth: 2,
                            tension: 0.4
                        },
                        {
                            label: '최종 자산 (복리 포함)',
                            data: data.map(d => d.total),
                            borderColor: '#10b981',
                            backgroundColor: 'rgba(16, 185, 129, 0.1)',
                            borderWidth: 3,
                            tension: 0.4,
                            fill: true
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            display: true,
                            position: 'top'
                        },
                        tooltip: {
                            callbacks: {
                                label: (context) => {
                                    return context.dataset.label + ': ' + formatMoney(context.parsed.y) + '원';
                                }
                            }
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: (value) => formatMoney(value) + '원'
                            }
                        }
                    }
                }
            });
        }

        // Event listeners for Monthly Compound
        mcEls.age.addEventListener('input', updateMonthlyCompound);
        mcEls.goal.addEventListener('input', updateMonthlyCompound);
        mcEls.saveRange.addEventListener('input', () => {
            mcEls.saveInput.value = mcEls.saveRange.value;
            updateMonthlyCompound();
        });
        mcEls.saveInput.addEventListener('input', updateMonthlyCompound);
        mcEls.rate.addEventListener('input', updateMonthlyCompound);
        mcEls.years.addEventListener('input', updateMonthlyCompound);

        // =================================================================

        const delayEls = {
            age: document.getElementById('delay-age'),
            goal: document.getElementById('delay-goal'),
            amount: document.getElementById('delay-amount'),
            years: document.getElementById('delay-years'),
            yearsText: document.getElementById('delay-years-text'),
            endAge: document.getElementById('delay-end-age'),
            delay: document.getElementById('delay-delay'),
            delayText: document.getElementById('delay-delay-text'),
            rate: document.getElementById('delay-rate'),
            rateText: document.getElementById('delay-rate-text'),
            nowAge: document.getElementById('delay-now-age'),
            nowYears: document.getElementById('delay-now-years'),
            nowMonthly: document.getElementById('delay-now-monthly'),
            nowTotal: document.getElementById('delay-now-total'),
            delayYears: document.getElementById('delay-delay-years'),
            delayAge: document.getElementById('delay-delay-age'),
            remainYears: document.getElementById('delay-remain-years'),
            delayMonthly: document.getElementById('delay-delay-monthly'),
            delayTotal: document.getElementById('delay-delay-total'),
            delayVal: document.getElementById('delay-val'),
            cost: document.getElementById('delay-cost')
        };

        function setDelayAmount(val) {
            delayEls.amount.value = val;
            calculateDelay();
        }

        function calculatePMT(target, r, n) {
            if (n <= 0) return 0;
            const monthlyRate = r / 100 / 12;
            const months = n * 12;
            const factor = ((Math.pow(1 + monthlyRate, months) - 1) / monthlyRate);
            return target / factor;
        }

        function calculateDelay() {
            const age = Number(delayEls.age.value) || 45;
            const goalAmount = Number(delayEls.amount.value) || 100000000;
            const years = Number(delayEls.years.value) || 20;
            const rate = Number(delayEls.rate.value) || 6;
            let delay = Number(delayEls.delay.value) || 5;

            // 지연 제한
            if (delay >= years) {
                delay = years - 1;
                delayEls.delay.value = delay;
            }

            delayEls.yearsText.textContent = years + '년';
            delayEls.endAge.textContent = age + years;
            delayEls.delayText.textContent = delay + '년 뒤 시작';
            delayEls.rateText.textContent = rate + '%';

            // 계산
            const nowPayment = calculatePMT(goalAmount, rate, years);
            const remainingYears = years - delay;
            const delayedPayment = calculatePMT(goalAmount, rate, remainingYears);

            const totalCostNow = nowPayment * 12 * years;
            const totalCostDelayed = delayedPayment * 12 * remainingYears;
            const lostMoney = totalCostDelayed - totalCostNow;

            // 렌더링
            delayEls.nowAge.textContent = age;
            delayEls.nowYears.textContent = years;
            delayEls.nowMonthly.textContent = formatMoney(nowPayment) + '원';
            delayEls.nowTotal.textContent = formatMoney(totalCostNow) + '원';

            delayEls.delayYears.textContent = delay;
            delayEls.delayAge.textContent = age + delay;
            delayEls.remainYears.textContent = remainingYears;
            delayEls.delayMonthly.textContent = formatMoney(delayedPayment) + '원';
            delayEls.delayTotal.textContent = formatMoney(totalCostDelayed) + '원';

            delayEls.delayVal.textContent = delay;
            delayEls.cost.textContent = formatMoney(lostMoney) + '원';
        }

        // 이벤트 리스너
        ['age', 'goal', 'amount', 'years', 'delay', 'rate'].forEach(key => {
            if (delayEls[key]) {
                delayEls[key].addEventListener('input', calculateDelay);
            }
        });

        //  ===== 은퇴 설계 계산기 =====
        const retEls = {
            age: document.getElementById('ret-age'),
            retireAge: document.getElementById('ret-retire-age'),
            pension: document.getElementById('ret-pension'),
            target: document.getElementById('ret-target'),
            saveYears: document.getElementById('ret-save-years'),
            saveBadge: document.getElementById('ret-save-badge'),
            saveRate: document.getElementById('ret-save-rate'),
            deferYears: document.getElementById('ret-defer-years'),
            deferBadge: document.getElementById('ret-defer-badge'),
            deferRate: document.getElementById('ret-defer-rate'),
            monthly: document.getElementById('ret-monthly'),
            saveText: document.getElementById('ret-save-text'),
            fvSave: document.getElementById('ret-fv-save'),
            target2: document.getElementById('ret-target-2'),
            pensionResult: document.getElementById('ret-pension-result'),
            step2: document.getElementById('ret-step-2'),
            alert: document.getElementById('ret-alert')
        };

        const DIVIDEND_RATE = 7;

        function calculateRetirement() {
            const age = Number(retEls.age.value) || 40;
            const retireAge = Number(retEls.retireAge.value) || 65;
            const monthlyPension = Number(retEls.pension.value) || 3000000;
            const saveYears = Number(retEls.saveYears.value) || 10;
            const saveRate = Number(retEls.saveRate.value) || 5;
            const deferYears = Number(retEls.deferYears.value) || 15;
            const deferRate = Number(retEls.deferRate.value) || 5;

            // 배지 업데이트
            retEls.saveBadge.textContent = saveYears + '년';
            retEls.deferBadge.textContent = deferYears + '년';

            // 목표 금액 계산
            const targetCorpus = (monthlyPension * 12) / (DIVIDEND_RATE / 100);
            retEls.target.textContent = formatMoney(targetCorpus) + '원';

            // 거치 후 필요한 금액
            const requiredFVSave = targetCorpus / Math.pow(1 + deferRate / 100, deferYears);

            // 월 저축액
            const monthlyRate = saveRate / 100 / 12;
            const totalMonths = saveYears * 12;
            const factor = (Math.pow(1 + monthlyRate, totalMonths) - 1) / monthlyRate;
            const requiredMonthlySave = requiredFVSave / factor;

            // 결과 렌더링
            retEls.monthly.textContent = formatMoney(requiredMonthlySave) + '원';
            retEls.saveText.textContent = saveYears;
            retEls.fvSave.textContent = formatMoney(requiredFVSave) + ' 달성';
            retEls.target2.textContent = formatMoney(targetCorpus) + ' 달성';
            retEls.pensionResult.textContent = '매월 ' + formatMoney(monthlyPension);

            // 2단계 표시 유무
            if (deferYears > 0) {
                retEls.step2.style.display = 'flex';
            } else {
                retEls.step2.style.display = 'none';
            }

            // 경고 메시지
            const totalPlanYears = saveYears + deferYears;
            const yearsUntilRetire = retireAge - age;
            if (totalPlanYears !== yearsUntilRetire) {
                retEls.alert.classList.remove('hidden');
            } else {
                retEls.alert.classList.add('hidden');
            }
        }

        // 은퇴 설계 이벤트 리스너
        Object.values(retEls).forEach(el => {
            if (el && el.addEventListener) el.addEventListener('input', calculateRetirement);
        });


        // 은퇴 설계 초기화 추가
        window.addEventListener('DOMContentLoaded', () => {
            calculateRetirement();
        });


    </script>
</body>

</html>